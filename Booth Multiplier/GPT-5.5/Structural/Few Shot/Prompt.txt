Example of Structural RTL coding style:

module full_adder(

input a,

input b,

input cin,

output sum,

output cout

);

assign sum = a ^ b ^ cin;

assign cout = (a & b) | (a & cin) | (b & cin);

endmodule

Using the same Structural RTL style shown above, generate synthesizable Verilog code for a 16-bit signed Booth Multiplier.

Requirements:

- Radix-2 Booth multiplication.

- Structural RTL only.

- Explicit module hierarchy.

- Booth encoder modules and partial-product modules should be instantiated structurally.

- No behavioral blocks.

- No multiplication operator (*).

- Target FPGA: xc7a100tcsg324-2.

- Output code only.