`timescale 1ns/1ps 
 
module barrel_shifter16_df ( 
    input  wire [15:0] in, 
    input  wire [3:0]  shift, 
    output wire [15:0] out 
); 
 
    // ------------------------------------------------- 
    // Stage 0 : shift by 1 
    // ------------------------------------------------- 
    wire [15:0] s0; 
    assign s0 = shift[0] ? {in[14:0], 1'b0} : in; 
 
    // ------------------------------------------------- 
    // Stage 1 : shift by 2 
    // ------------------------------------------------- 
    wire [15:0] s1; 
    assign s1 = shift[1] ? {s0[13:0], 2'b00} : s0; 
 
    // ------------------------------------------------- 
    // Stage 2 : shift by 4 
    // ------------------------------------------------- 
    wire [15:0] s2; 
    assign s2 = shift[2] ? {s1[11:0], 4'b0000} : s1; 

 
    // ------------------------------------------------- 
    // Stage 3 : shift by 8 
    // ------------------------------------------------- 
    wire [15:0] s3; 
    assign s3 = shift[3] ? {s2[7:0], 8'b00000000} : s2; 
 
    // Final output 
    assign out = s3; 
 
endmodule